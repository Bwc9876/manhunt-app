import React from "react";
import { GameSettings } from "@/bindings";

export default function CreateGame({ settings }: { settings: GameSettings }) {
    return <div className="flex flex-col items-center justify-center p-3.5"></div>;
}
